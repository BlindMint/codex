# Environment variables for release automation
# Copy this file to .env and fill in your values

# GitLab Personal Access Token (get from: GitLab → User Settings → Access Tokens)
# Required scope: api
GITLAB_TOKEN=your_gitlab_token_here

# GitLab Project ID (get from: Project → Settings → General → Project ID)
GITLAB_PROJECT_ID=your_project_id_here

# Optional: Debug mode for release script (set to 'true' for verbose output)
# DEBUG=true